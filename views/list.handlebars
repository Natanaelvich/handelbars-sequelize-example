<link rel="stylesheet" href="list.css">
<div class="list">
    <div class="title-page">
        <h1>Posts</h1>
        <img src="images/superpotencia.svg" alt="hi" width="50">
    </div>
    {{#each post}}
    <div class="box">
        <h1>{{dataValues.title}}</h1>
        <p>{{dataValues.description}}</p>
    </div>
    {{/each}}

</div>